[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not integration"
asyncio_mode = auto
markers =
  integration: marks tests that require live AWS (S3, DynamoDB, Lambda). Run with: pytest -m integration
