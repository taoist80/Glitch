# Story book

Summaries and key details for long-running stories (characters, plot beats, tone).  
Poet reads this for continuity and writes to it via the update_story_book tool when you ask to record.

Stored in the same S3 bucket as poet-soul.md when deployed; this file is the local fallback.
